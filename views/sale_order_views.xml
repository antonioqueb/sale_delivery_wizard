<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Sale Order Form: Delivery Cockpit -->
    <record id="sale_order_form_delivery_cockpit" model="ir.ui.view">
        <field name="name">sale.order.form.delivery.cockpit</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="priority">99</field>
        <field name="arch" type="xml">

            <!-- Acciones principales -->
            <xpath expr="//header" position="inside">
                <button name="action_open_delivery_wizard"
                        string="Entregar"
                        type="object"
                        class="btn-primary"
                        invisible="state not in ('sale', 'done')"
                        groups="sale_delivery_wizard.group_delivery_user"/>

                <button name="action_open_return_wizard"
                        string="Devolución"
                        type="object"
                        class="btn-warning"
                        invisible="state not in ('sale', 'done')"
                        groups="sale_delivery_wizard.group_delivery_user"/>

                <button name="action_open_swap_wizard"
                        string="Swap"
                        type="object"
                        invisible="state not in ('sale', 'done')"
                        groups="sale_delivery_wizard.group_delivery_user"/>
            </xpath>

            <!-- Smart buttons -->
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_pick_tickets"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-clipboard"
                        invisible="x_pick_ticket_count == 0">
                    <div class="o_stat_info">
                        <span class="o_stat_value">
                            <field name="x_pick_ticket_count"/>
                            <span invisible="x_pick_ticket_open_count == 0"
                                  class="text-warning">
                                (<field name="x_pick_ticket_open_count"/> abiertos)
                            </span>
                        </span>
                        <span class="o_stat_text">Pick Tickets</span>
                    </div>
                </button>

                <button name="action_view_remissions"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-truck"
                        invisible="x_remission_count == 0">
                    <field name="x_remission_count"
                           widget="statinfo"
                           string="Remisiones"/>
                </button>

                <button name="action_view_returns"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-undo"
                        invisible="x_return_count == 0">
                    <field name="x_return_count"
                           widget="statinfo"
                           string="Devoluciones"/>
                </button>

                <button name="action_view_redeliveries"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-refresh"
                        invisible="x_redelivery_count == 0">
                    <field name="x_redelivery_count"
                           widget="statinfo"
                           string="Reentregas"/>
                </button>
            </xpath>

            <!-- KPIs superiores sin métricas de origen -->
            <xpath expr="//notebook" position="before">
                <div class="som_kpi_strip" invisible="state not in ('sale', 'done')">
                    <div class="som_kpi">
                        <span class="som_kpi_value">
                            <field name="x_total_current_demand_qty" class="d-inline"/>
                        </span>
                        <span class="som_kpi_label">Solicitado Actual</span>
                    </div>

                    <div class="som_kpi">
                        <span class="som_kpi_value som_kpi_value--success">
                            <field name="x_total_delivered_net_qty" class="d-inline"/>
                        </span>
                        <span class="som_kpi_label">Entregado Neto</span>
                    </div>

                    <div class="som_kpi">
                        <span class="som_kpi_value som_kpi_value--warn">
                            <field name="x_total_returned_qty" class="d-inline"/>
                        </span>
                        <span class="som_kpi_label">Devuelto</span>
                    </div>

                    <div class="som_kpi">
                        <span class="som_kpi_value som_kpi_value--accent">
                            <field name="x_fulfillment_net_pct"
                                   widget="float"
                                   digits="[3,1]"
                                   class="d-inline"/>%
                        </span>
                        <span class="som_kpi_label">Cumplimiento</span>
                    </div>
                </div>
            </xpath>

            <!-- Historial de entregas -->
            <xpath expr="//notebook" position="inside">
                <page string="Historial de Entregas"
                      name="delivery_history"
                      invisible="x_delivery_document_count == 0">
                    <field name="delivery_document_ids" readonly="1">
                        <list>
                            <field name="name"/>
                            <field name="document_type"/>
                            <field name="state"
                                   decoration-success="state == 'confirmed'"
                                   decoration-warning="state == 'prepared'"
                                   decoration-danger="state == 'cancelled'"
                                   widget="badge"/>
                            <field name="remission_number"/>
                            <field name="total_qty"/>
                            <field name="return_reason_id"/>
                            <field name="return_action"/>
                            <field name="delivery_date"/>
                            <field name="create_date"/>
                        </list>
                    </field>
                </page>
            </xpath>

            <!--
                Limpieza visual de líneas de venta.

                Se eliminan de la tabla principal todos los campos de origen:
                - x_origin_demand_qty
                - x_origin_demand_locked
                - x_origin_demand_source
                - x_origin_demand_locked_at
                - x_origin_pending_qty
                - x_overdelivered_origin_qty

                También se dejan fuera métricas duplicadas de pendiente,
                fulfillment y estado de entrega.

                Este XML no borra campos del modelo.
                Solo evita que aparezcan como columnas en la orden de venta.
            -->
        </field>
    </record>

    <!-- Devuelto por línea vive en la pestaña Logística (stock_transit_allocation) -->
    <record id="sale_order_form_delivery_logistics" model="ir.ui.view">
        <field name="name">sale.order.form.delivery.logistics</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="stock_transit_allocation.view_order_form_transit_inherit"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='logistics_lines']//list/field[@name='qty_delivered']" position="after">
                <field name="x_returned_qty"
                       string="Devuelto"
                       readonly="1"/>
            </xpath>
        </field>
    </record>

    <!-- Campos técnicos requeridos por modificadores, smart buttons y KPIs -->
    <record id="sale_order_form_delivery_fields" model="ir.ui.view">
        <field name="name">sale.order.form.delivery.fields</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="priority">100</field>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="x_delivery_document_count" invisible="1"/>
                <field name="x_remission_count" invisible="1"/>
                <field name="x_return_count" invisible="1"/>
                <field name="x_pick_ticket_count" invisible="1"/>
                <field name="x_pick_ticket_open_count" invisible="1"/>
                <field name="x_redelivery_count" invisible="1"/>
                <field name="x_redelivery_pending_count" invisible="1"/>

                <field name="x_total_current_demand_qty" invisible="1"/>
                <field name="x_total_delivered_net_qty" invisible="1"/>
                <field name="x_total_returned_qty" invisible="1"/>
                <field name="x_fulfillment_net_pct" invisible="1"/>
            </xpath>
        </field>
    </record>
</odoo>